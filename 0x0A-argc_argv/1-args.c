#include <stdio.h>

/**
 * main - prints number of arguments on the command line
 * @argc: number of arguments on the command line
 * @argv: array holding the arguments on the command line
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
